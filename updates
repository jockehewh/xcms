latest update:

Adds:
  -new context menu to quickly add CSS and duplicate a node.
  -The css is now moved inline after editing.
Changes:
  -The css is now applied after closing the editor.
BugFixes:
  -fixed a bug where the "in page" css was bleeding out at re-edit time.
  